# 安全运营检测实验室（三虚拟机攻击模拟）项目总结

> 这是整个安全运营方向最核心的成果：用三台虚拟机搭了一个攻击模拟 + 检测闭环，完整跑通了「复现攻击 → 检测 → 告警 → 攻击链还原 → ATT&CK 映射」。

## 一、项目概述

在 VMware 里搭了一个**安全运营检测实验室**，用开源 SIEM/XDR 平台 Wazuh 作为检测核心，模拟真实的 SSH 暴力破解攻击，验证了"检测 → 分析 → 响应"的完整安全运营链路。

## 二、架构（三台虚拟机）

| 虚拟机 | 角色 | 系统 | 关键组件 |
|---|---|---|---|
| **检测机** | Wazuh 服务器（SIEM/XDR）| Ubuntu 24.04（8GB）| wazuh-indexer / manager / dashboard / filebeat |
| **攻击机** | 攻击者 | Kali Linux | hydra（暴力破解工具）|
| **靶机** | 被监控目标 | Ubuntu 24.04（2GB）| wazuh-agent（接入 Wazuh）|

```
Kali(攻击机)  --SSH暴力破解-->  target(靶机/wazuh-agent)
                                    |
                              日志上报(1514)
                                    v
                          检测机(Wazuh 服务器)
                                    |
                              规则匹配 + 告警
                                    v
                          dashboard 告警 + MITRE 映射
```

## 三、攻击模拟完整流程

1. **接入 agent**：靶机装 wazuh-agent，接入 Wazuh 检测机（日志实时上报）
2. **复现攻击**：Kali 用 hydra 对靶机 SSH 端口发起暴力破解（1434 万条密码字典）
3. **检测**：Wazuh 的 agent 采集靶机 `auth.log`（SSH 认证失败日志）
4. **告警**：规则引擎触发 SSH 暴力破解规则（rule 5758 / 2502 / 40111）
5. **攻击链还原**：告警里完整记录 攻击源 IP → 目标账户 → 服务 → 时间线
6. **ATT&CK 映射**：自动映射 MITRE **T1110（Brute Force / Credential Access）**

## 四、真实成果（告警证据）

- **攻击链**：`192.168.254.135`（Kali）→ `ubuntu-target`（192.168.254.143）的 `target` 账户，SSH 服务
- **触发规则**：
  - `5758` Maximum authentication attempts exceeded（level 8）
  - `2502` User missed the password more than one time（level 10）
  - `40111` Multiple authentication failures（level 10）
- **MITRE ATT&CK**：T1110 - Brute Force / Credential Access（凭据访问）
- **合规映射**：PCI DSS、HIPAA、NIST 800-53、GDPR 自动对应

## 五、体现的安全运营能力

| 能力 | 对应本项目 |
|---|---|
| 攻击理解 | 用 hydra 复现 SSH 暴力破解 |
| 检测配置 | 接入 wazuh-agent、配置日志采集 |
| 告警研判 | 读懂 3 条规则、还原攻击链 |
| 攻击溯源 | 从告警还原 攻击源→目标→账户 |
| ATT&CK 映射 | T1110 / Credential Access |
| 报告能力 | 输出研判结论 + 处置建议 |

## 六、技术栈

- **检测**：Wazuh 4.10（indexer + manager + dashboard + filebeat）
- **攻击**：Kali Linux + hydra
- **日志**：auth.log（SSH 认证日志）、journald
- **规则**：Wazuh 内置规则 5758/2502/40111
- **框架**：MITRE ATT&CK

## 七、简历项目描述（可直接用）

> **安全运营检测实验室（Wazuh SIEM）**
> 用三台虚拟机（Wazuh 服务器 / Kali 攻击机 / Ubuntu 靶机）搭建安全运营检测环境：靶机接入 wazuh-agent，Kali 用 hydra 复现 SSH 暴力破解，Wazuh 规则引擎触发告警（rule 5758/2502/40111），完成攻击链还原（源IP→目标账户→服务）并映射 MITRE ATT&CK T1110（Brute Force）。跑通了「检测 → 分析 → 响应」完整链路。

## 八、后续可扩展（进阶）

- 增加攻击场景：Web 攻击（SQL注入/DVWA）、Webshell、横向移动、挖矿
- 编写自定义 Sigma/Wazuh 规则，降低误报
- 接入 SOAR 自动化响应（自动封禁源 IP）
- 输出正式的安全事件分析报告
